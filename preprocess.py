from model.utils.trainingScr.run_training import _preprocess
# Preprocess script for preprocessing the data, it will generate a log file in the specified path with the results of the preprocessing.
if __name__ == "__main__":
    _preprocess(path="model/conf")